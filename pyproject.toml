[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "soda_citron"
version = "0.1.0"
description = "SODA-CitrON: Static Object Data Association by Clustering Multi-Modal Sensor Detections Online"
authors = [{ name="Jan Nausner", email="jan.nausner@ait.ac.at" }]
readme = "README.md"
requires-python = ">=3.11"
license = {file = "LICENSE"}

# Runtime dependencies for installation via pip
dependencies = [
    "numpy",
    "scipy",
    "river==0.25",
    "python-ulid",
]

# Optional dependencies
[project.optional-dependencies]
example = [
    "jupyter",
    "shapely",
    "geopandas",
    "plotly",
]

# Links that show in package metadata
[project.urls]
Homepage = "https://github.com/th3sh3ph3rd/SODA-CitrON"
Repository = "https://github.com/th3sh3ph3rd/SODA-CitrON.git"
Issues = "https://github.com/th3sh3ph3rd/SODA-CitrON/issues"

# Package discovery using src/ layout
[tool.setuptools.packages.find]
where = ["src"]